name: healthmate_app
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/healthmate_icon.png"

flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/healthmate_icon.png
    - assets/icons/home_healthmate_icon.png
